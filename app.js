function show(id){document.querySelectorAll(".page").forEach(p=>p.classList.remove("active"));document.getElementById(id).classList.add("active");scrollTo(0,0)}document.querySelectorAll(".stop").forEach(b=>b.onclick=()=>show("stop"+b.dataset.n));document.querySelectorAll(".question").forEach(q=>q.querySelectorAll("button").forEach((b,i)=>b.onclick=()=>{q.querySelectorAll("button").forEach(x=>x.classList.remove("good","bad"));let ok=i==+q.dataset.correct;b.classList.add(ok?"good":"bad");q.querySelector(".feedback").innerHTML=ok?"<b>Best supported.</b> "+q.dataset.explain:"Look again at the weather variables and choose the explanation that best fits the plant response.";q.querySelector(".climate").classList.toggle("hidden",!ok);q.closest(".page").querySelector(".return").classList.remove("hidden")}));document.querySelectorAll(".return").forEach(b=>b.onclick=()=>{let n=+b.dataset.n;if(n===4){show("finish");return}show("map");let next=document.querySelector('.stop[data-n="'+(n+1)+'"]');document.getElementById("arrival").textContent="Follow the boots along the path…";document.getElementById("boots").className="boots b"+(n+1);setTimeout(()=>{next.disabled=false;document.getElementById("arrival").textContent="Stop "+(n+1)+" is ready.";},matchMedia("(prefers-reduced-motion: reduce)").matches?50:1550)});show("intro");